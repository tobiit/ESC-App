import { DesignToken } from 'style-dictionary/types';

export interface TypographyUserAgentSsot {
	core: {
		internal: {
			// eslint-disable-next-line @typescript-eslint/naming-convention
			'font-size': {
				// eslint-disable-next-line @typescript-eslint/naming-convention
				'user-agent': DesignToken;
			};
		};
	};
}
