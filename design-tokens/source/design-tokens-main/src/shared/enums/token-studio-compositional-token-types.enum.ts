import { DtcgCompositeTokenTypes } from './dtcg-composite-token-types.enum';

export enum TokenStudioCompositionalTokenTypes {
	boxShadow = 'boxShadow',
	composition = 'composition',
	typography = DtcgCompositeTokenTypes.typography,
}
